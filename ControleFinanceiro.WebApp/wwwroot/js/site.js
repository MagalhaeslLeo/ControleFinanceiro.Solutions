$('.showModalDelete').on('click', function () {
    var targetId = $(this).data('target');
    $(targetId).modal('show');
})
$('.showModalButton2').on('click', function () {
    var targetId = $(this).data('target');
    $(targetId).modal('show');
})
$('.showModalButton3').on('click', function () {
    var targetId = $(this).data('target');
    $(targetId).modal('show');
})
$('.showModalButton4').on('click', function () {
    var targetId = $(this).data('target');
    $(targetId).modal('show');
})
